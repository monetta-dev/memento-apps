/**
 * LINE Works Bot API によるメッセージ送信（スタブ実装）
 * OAuth 2.0 フローが複雑なため、将来実装予定
 */

export async function sendLineWorksMessage(
    _accessToken: string,
    _userId: string,
    _message: string
): Promise<void> {
    // TODO: LINE Works Bot API を実装する
    // 参考: https://developers.worksmobile.com/jp/reference/bot-send-message-to-user
    throw new Error('LINE Works 連携は準備中です。');
}
